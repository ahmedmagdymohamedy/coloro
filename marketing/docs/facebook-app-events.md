# Meta (Facebook) App Events — DONE

**Status: created and integrated, 29 Aug 2026.**

| | |
|---|---|
| App name | **Coloro** |
| **App ID** | **1531517322075227** |
| **Client token** | **03967af133d8941fb7f80ce052a3d9d5** |
| Business portfolio | **Megz Games** (unverified) |
| Use case | Create & manage app ads with Meta Ads Manager |
| Platform declared | **Android → Google Play Store**, `com.megz.coloro` (29 Aug 2026) |
| Contact email | megzga@gmail.com |

Creating it from the **Megz Games business account** worked where the
personal account did not — that account never demanded the password
re-authentication that blocked the two earlier attempts.

## What shipped in the app

- `facebook_app_events: ^0.30.5` added
- `android/app/src/main/res/values/strings.xml` — app id + client token
- `AndroidManifest.xml` — `com.facebook.sdk.ApplicationId` / `ClientToken`
- `lib/core/analytics/facebook_sink.dart` — the sink
- `lib/main.dart` — `_initMetaEvents()`, Android-gated

**Android only, on purpose.** The credentials are deliberately absent from
`ios/Runner/Info.plist`, so the SDK has no app id to read and never
initialises on iOS. `Info.plist` records that App Store Connect already
refused one submission of this app over tracking signals; turning Meta on for
iOS needs a real ATT + UMP consent flow and its own submission.

Both platforms were verified building with the SDK present:
Android AAB 55M → 56M, iOS IPA 25.7MB → 30.6MB (the FBSDK on iOS, inert).

> **Before the next iOS submission:** the binary now contains the Facebook
> SDK even though it never initialises. Keep App Privacy answering "no
> tracking" and do not add `NSUserTrackingUsageDescription`, or the earlier
> rejection can repeat.

## Not yet shipped

This integration is in the tree but **not in a released build** — Android
1.0.7 went live before it. It rides the next release.

---

## Step 1 — create the app ✅ DONE

1. Go to <https://developers.facebook.com/apps/creation/>
2. Name `Coloro`, email `ahmedmagdymohamedy@gmail.com` → **Next**
3. Use case: **Create and manage app ads through Meta Ads Manager** → **Next**
4. Business portfolio: skip → **Next** → **Next** → **Create app**
5. Enter the password when asked.

Then copy two values from **App settings → Basic**:
- **App ID** (a long number)
- **Client Token** (under *Client token*)

## Step 1b — declare the platform ✅ DONE (29 Aug 2026)

Creating the app is not enough. Until a **platform** is declared under
*App settings → Basic*, Meta has no package name to bind incoming events
to, so they arrive unattached to the Play listing and cannot drive
app-install campaign optimisation later.

Declared: **Android → Google Play Store**

| Field | Value |
|---|---|
| Package Names | `com.megz.coloro` |
| Class name | `com.megz.coloro.MainActivity` |
| Install Referrer Decryption Key | auto-generated by Meta on save |

Both values are checked against source: `applicationId` in
`android/app/build.gradle.kts` and the `package` line of
`android/app/src/main/kotlin/com/megz/coloro/MainActivity.kt`.

The Install Referrer Decryption Key appears by itself once the Google Play
Store entry saves. It is what lets Meta read the Play install referrer and
attribute an install to a click — leave it alone.

### Two traps in this form

1. **Package Names is a typeahead, not a text box.** Typing the value and
   pressing Enter looks right but commits nothing, and the save then fails
   with *"A package name is required if you specify a class name."* You have
   to click the suggestion in the dropdown until the value becomes a chip
   with an `✕` on it. Committing the chip also shifts the row up ~30px, so a
   click aimed at *Class name* from a pre-chip screenshot lands on nothing.
2. **A save with validation errors fails silently.** No toast, no scroll to
   the bad field — the button simply does nothing and the whole form is lost
   on the next reload. A successful save shows a *"Changes saved"* toast.
   Treat the absence of that toast as a failure, and re-check by reloading.

### Why iOS is not declared

The iOS platform form requires an **iPhone Store ID** and an **iPad Store
ID**, and those do not exist yet — Coloro is not live on the App Store
(confirmed against `itunes.apple.com/lookup?bundleId=com.megz.coloro`,
which returns `resultCount: 0`). Attempting it is what silently blocked the
first save.

This costs nothing today, because the Facebook SDK is deliberately inert on
iOS anyway — see the header of `lib/core/analytics/facebook_sink.dart`.
Declare the iOS platform in the same form when both of these are true:
the app is live on the App Store (so a Store ID exists), and the SDK has
actually been switched on for iOS behind a real ATT + UMP consent flow.

---

## Step 2 — add the SDK ✅ DONE

```sh
flutter pub add facebook_app_events
```

**Android only.** Do not add it to the iOS target yet, for two specific
reasons recorded in this repo:

1. `ios/` is deliberately **Swift Package Manager only, with no Podfile**
   (`scripts/README.md`). `facebook_app_events` would drag CocoaPods back
   into the iOS build, which is not something to discover on a release night.
2. `ios/Runner/Info.plist` carries a comment recording that App Store Connect
   **already refused a submission once** over tracking signals, and the fix
   was removing `NSUserTrackingUsageDescription` entirely. The Facebook SDK
   reintroduces exactly that class of signal. Adding it to iOS needs a real
   ATT + UMP consent flow and its own submission, not a drive-by.

Android wiring — `android/app/src/main/AndroidManifest.xml`, inside
`<application>`:

```xml
<meta-data android:name="com.facebook.sdk.ApplicationId"
           android:value="@string/facebook_app_id" />
<meta-data android:name="com.facebook.sdk.ClientToken"
           android:value="@string/facebook_client_token" />
<!-- Manual init: the game decides when the SDK starts, so a missing or
     wrong id can never take the app down on launch. -->
<meta-data android:name="com.facebook.sdk.AutoInitEnabled"
           android:value="false" />
```

and `android/app/src/main/res/values/strings.xml`:

```xml
<resources>
  <string name="facebook_app_id">PASTE_APP_ID</string>
  <string name="facebook_client_token">PASTE_CLIENT_TOKEN</string>
</resources>
```

## Step 3 — switch it on ✅ DONE

Everything else is already written. `lib/core/analytics/meta_events.dart`
defines the funnel and a `MetaEventSink` transport;
`lib/core/analytics/analytics_service.dart` already fans every relevant event
out to it. The layer is inert until `configure()` is called, which is why it
is safe in today's release.

Add the sink and call it from `main.dart`:

```dart
// lib/core/analytics/facebook_sink.dart
import 'dart:io';
import 'package:facebook_app_events/facebook_app_events.dart';
import 'meta_events.dart';

class FacebookSink implements MetaEventSink {
  final _fb = FacebookAppEvents();
  @override
  void logEvent(String name, Map<String, Object> parameters) {
    _fb.logEvent(name: name, parameters: parameters);
  }
}

// in main.dart, after Firebase init:
if (!kIsWeb && Platform.isAndroid) {
  MetaEvents.instance.configure(FacebookSink());
}
```

Then verify in **Events Manager → Coloro → Test Events** with a debug build.

## The events Meta receives

Deliberately a small, standard-named subset. Meta's ad models have
cross-advertiser priors for standard event names; the game's 365 per-level
Firebase names would mean nothing to them and would only dilute the signal.

| Meta event | Fired when | Why it is in the set |
|---|---|---|
| `fb_mobile_level_achieved` | any level completed | progression depth |
| `fb_mobile_tutorial_completion` | level 1 completed | **activation** — the install-campaign optimisation target until volume allows deeper |
| `fb_mobile_ad_impression` | any full-screen ad shown | the value event: ads are the only revenue |
| `fb_mobile_rewarded_video_completed` | rewarded ad watched | strongest single predictor of a high-value player |
| `fb_mobile_session_milestone` | retention checkpoints | feeds lookalikes of players who stay |

## Note on ads

Ahmed's Meta **ads** account is banned, so none of this is for running Meta
campaigns today. It exists so that the attribution and event history are
already accumulating if that ever changes — an app that starts collecting
events on day one is worth far more to a future campaign than one wired up
the week the campaign launches.
